---
description: "Test-Driven Development - RED-GREEN-REFACTOR cycle"
---
Load the `test-driven-development` skill. Enforce the TDD cycle: write a failing test first (RED), watch it fail, write minimal code to pass (GREEN), then refactor. No production code without a failing test first. Delete code written before tests.
