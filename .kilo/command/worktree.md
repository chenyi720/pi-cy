---
description: "Create isolated git worktree for feature work"
---
Load the `using-git-worktrees` skill. Create an isolated workspace on a new branch via git worktree. Run project setup, verify clean test baseline. Enables parallel development without interference.
